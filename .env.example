# Temco Customer Portal Environment Configuration

# API URL - In production, leave empty to use Nginx proxy (/api)
# For local dev with `npm run dev`, the Vite proxy handles API routing.
VITE_API_URL=
